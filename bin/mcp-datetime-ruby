#!/usr/bin/env ruby
# frozen_string_literal: true

# Try to load from gem first, fall back to relative path for development
begin
  require "mcp/datetime"
rescue LoadError
  require_relative "../lib/mcp/datetime"
end

# Run the MCP DateTime server
server = MCP::DateTime::Server.new
server.run 